/**
 * Re-exports all configuration utilities, types, and classes for the Discord bot.
 * 
 * Use this module to access type-safe, validated, and application-wide settings
 * loaded from environment variables or other configuration sources. This ensures
 * a central and consistent access point for all configuration values required
 * throughout the codebase.
 * 
 * @module config
 */
export * from './bot.config';